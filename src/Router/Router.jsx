import { createBrowserRouter } from "react-router-dom";
const Router = createBrowserRouter([
    {
      path: "/",
      element: <div className="text-5xl text-red-500">Hello world!</div>,
    },
  ]);
  

export default Router;